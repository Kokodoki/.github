## 1. Reusable Workflow

- [x] 1.1 Crear `.github/workflows/infra-terraform-azure.yml` con los jobs `validate`, `plan` y `apply` usando autenticación OIDC con `azure/login@v2`

## 2. Documentación

- [x] 2.1 Crear `docs/infra-terraform-azure.md` con descripción del workflow, ejemplo de uso, tablas de inputs/outputs/secrets e instrucciones de configuración de OIDC en Azure

## 3. Workflow Template

- [x] 3.1 Crear `workflow-templates/infra-terraform-azure.yml` con el template de uso del workflow
- [x] 3.2 Crear `workflow-templates/infra-terraform-azure.properties.json` con los metadatos del template
